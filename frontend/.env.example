# Next.js rewrite가 요청을 전달할 Spring Boot 서버 주소입니다.
BACKEND_URL=http://localhost:8080

# 비워 두면 브라우저는 동일 출처의 /api 경로를 사용합니다.
NEXT_PUBLIC_API_BASE_URL=

# true일 때만 화면용 목 데이터를 사용합니다. 기본 개발·배포 환경은 실제 API를 사용합니다.
NEXT_PUBLIC_USE_MOCK=false
