# ===================================================================
# 1. 공통 및 로컬 개발 환경 설정
# ===================================================================
spring:
  application:
    name: moigo
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 11MB
  datasource:
    url: ${DB_URL:jdbc:h2:mem:moigo;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE}
    username: ${DB_USERNAME:sa}
    password: ${DB_PASSWORD:}
  jpa:
    hibernate:
      ddl-auto: ${JPA_DDL_AUTO:validate}
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  cloud:
    aws:
      region:
        static: ${AWS_REGION:ap-northeast-2}

app:
  jwt:
    secret: ${JWT_SECRET:dev-only-dummy-secret-change-me}
    access-token-expiration-seconds: ${JWT_ACCESS_EXPIRATION:3600}
    refresh-token-expiration-seconds: ${JWT_REFRESH_EXPIRATION:1209600}
  oauth:
    cookie-secure: ${OAUTH_COOKIE_SECURE:false}
  aws:
    s3:
      bucket: ${S3_BUCKET_NAME:moigo-local}
      public-url: ${S3_PUBLIC_URL:http://localhost:4566/moigo-local}

kakao:
  local:
    rest-api-key: ${KAKAO_REST_API_KEY:dummy-kakao-rest-api-key}
  oauth:
    client-id: ${KAKAO_CLIENT_ID:dummy-kakao-client-id}
    client-secret: ${KAKAO_CLIENT_SECRET:dummy-kakao-client-secret}
    redirect-uri: ${KAKAO_REDIRECT_URI:http://localhost:8080/login/oauth2/code/kakao}

server:
  port: ${SERVER_PORT:8080}

# -------------------------------------------------------------------
# Actuator 헬스체크 엔드포인트 설정
# -------------------------------------------------------------------
management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when_authorized

---
# ===================================================================
# 2. 운영 환경 설정 (AWS RDS & Docker/EC2 배포용)
# ===================================================================
spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

app:
  jwt:
    secret: ${JWT_SECRET}
  oauth:
    cookie-secure: ${OAUTH_COOKIE_SECURE:true}
  aws:
    s3:
      bucket: ${S3_BUCKET_NAME}
      public-url: ${S3_PUBLIC_URL}

kakao:
  local:
    rest-api-key: ${KAKAO_REST_API_KEY}
  oauth:
    client-id: ${KAKAO_CLIENT_ID}
    client-secret: ${KAKAO_CLIENT_SECRET}
    redirect-uri: ${KAKAO_REDIRECT_URI}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: never

---
# ===================================================================
# 3. RDS TLS 설정
# ===================================================================
spring:
  config:
    activate:
      on-profile: rds
    import: configtree:/run/secrets/
  datasource:
    hikari:
      data-source-properties:
        trustCertificateKeyStoreUrl: file:/app/certs/rds-truststore.p12
        trustCertificateKeyStoreType: PKCS12
        fallbackToSystemTrustStore: false

---
# ===================================================================
# 4. Gradle test 및 CI 환경을 위한 테스트 전용
# ===================================================================
spring:
  config:
    activate:
      on-profile: test
  cloud:
    aws:
      credentials:
        access-key: test
        secret-key: test
  datasource:
    url: jdbc:h2:mem:moigo;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: none

---
# ===================================================================
# 5. 로컬 Docker 개발 환경(MySQL & LocalStack) 설정
# ===================================================================
spring:
  config:
    activate:
      on-profile: local
  cloud:
    aws:
      credentials:
        access-key: test
        secret-key: test
      region:
        static: ${AWS_REGION:us-east-1}
      s3:
        endpoint: http://localhost:4566
        path-style-access-enabled: true

app:
  aws:
    s3:
      bucket: moigo-local
